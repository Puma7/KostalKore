[pytest]
testpaths = tests
pythonpath = .
addopts = --import-mode=importlib --cov --cov-report=term-missing --cov-fail-under=100
filterwarnings =
    ignore::DeprecationWarning
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
norecursedirs =
    pykoplenti-master
